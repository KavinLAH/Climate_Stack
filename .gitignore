# Local environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Dependencies
node_modules/

# OS files
.DS_Store

# Native folders (already ignored in app, but good to have here too)
pestcast-app/ios
pestcast-app/android
pestcast-app/.expo
